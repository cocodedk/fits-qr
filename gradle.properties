org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# The JDK comes from JAVA_HOME (CI sets it via actions/setup-java). Do not pin
# org.gradle.java.home here — an absolute path from one machine breaks every other one.
