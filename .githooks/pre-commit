#!/bin/sh
set -eu

# Fast static checks ONLY. The full build gate runs at pre-push, not on every commit,
# so the commit loop stays fast.
[ -f "$HOME/.fits-env.sh" ] && . "$HOME/.fits-env.sh"
echo "pre-commit: running fast lint (./gradlew lintDebug)..."
./gradlew lintDebug --quiet </dev/null
